{
  "name": "convince",
  "version": "0.1.2",
  "description": "Type safe schema validation library.",
  "author": "Mark Murray",
  "license": "UNLICENSED",
  "repository": "git@github.com:mmmurray/convince.git",
  "keywords": [
    "schema",
    "validation",
    "typescript"
  ],
  "files": [
    "lib"
  ],
  "sideEffects": false,
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "format": "prettier --write 'src/**/*.ts'",
    "test:coverage": "jest --coverage",
    "test:format": "prettier --check 'src/**/*.ts'",
    "test:type": "tsc --noEmit",
    "test:watch": "jest --watch",
    "test": "yarn test:type && yarn test:format && yarn test:coverage"
  },
  "devDependencies": {
    "@types/jest": "^29.2.4",
    "jest": "^29.3.1",
    "prettier": "^2.8.1",
    "ts-jest": "^29.0.3",
    "typescript": "^4.9.4"
  }
}
